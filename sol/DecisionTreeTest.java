package sol;

import static org.junit.Assert.assertEquals;
import static org.junit.Assert.assertThrows;
import static org.junit.Assert.assertTrue;
import static org.junit.Assert.assertFalse;

import org.junit.Assert;
import org.junit.Test;
import org.junit.Before;

/*
visualizer hasn't been working for me so working on writing tests in this class using parse and the csv files
currently tryng to test methods on  a smaller set of dataset called fruittesting
 */
public class DecisionTreeTest {
    //make your own data set in the same csv format and call generateTree on
    // Constructor for DecisionTreeTest tester class - don't need to add anything in here!
    //parse("/Users/angie/Desktop/cs200/decision-tree-icebear200-poseydurling/sol/fruittesting.csv")
    public DecisionTreeTest() {
    }

    @Test
    public void testExample() { 
        assertEquals(6, 1 + 2 + 3);
    }
    
    // TODO: Add your tests here!
    
}
